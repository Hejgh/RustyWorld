[package]
name = "minecraft-like-bevy"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.13.0"
fastnoise-lite = "1.1.1"
rand = "0.8.5"

# Remove bevy-inspector-egui for now (causing version conflicts)
# bevy-inspector-egui = "0.25.0"
